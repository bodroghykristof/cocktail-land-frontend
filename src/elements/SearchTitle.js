import styled from 'styled-components';

const SearchTitle = styled.h3`
  border: 3px solid #ebca6c;
  border-radius: 6px;
  background-color: rgba(238, 212, 142, 0.7);

  max-width: 40%;

  margin: auto;
  padding: 10px;
`;

export default SearchTitle;
